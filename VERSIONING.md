# IMGW-OSMET — schemat wersjonowania

## Format: `MAJOR.MINOR.PATCH[-bBUILD]`

| Segment | Kiedy rośnie | Przykład |
|---------|-------------|---------|
| MAJOR   | Przełomowa zmiana architektury lub całkowity redesign | 3.0.0 |
| MINOR   | Nowa funkcjonalność (nowy moduł, nowy widok, nowa integracja) | 2.5.0 |
| PATCH   | Naprawy błędów, poprawki UX, drobne usprawnienia | 2.5.1 |
| `-bN`   | Build wewnętrzny — wersja do testów, nie do wdrożenia produkcyjnego | 2.5.0-b1 |

## Zasady

- **ZIP do testu** → zawsze `-bN` (b1, b2, …). Gdy zaakceptowany → PATCH lub MINOR bez przyrostka.
- Wersja widoczna w UI (lewy górny róg), w `/api/` (pole `version`), w plikach ZIP i w README.
- Plik `VERSION` w root projektu zawiera aktualną wersję (jedna linia).
- Każda wersja ma wpis w ROADMAP.md.

## Historia milestones

| Wersja  | Data       | Co ważnego |
|---------|------------|------------|
| 2.4.3   | 2026-05-08 | Fix MeteoAlarm 2026 (CZ/DE/LT atom namespace) |
| 2.4.4   | 2026-05-10 | Rebranding IMGW-OSMET, logo, severity fix, tooltip fix |
| 2.5.0   | 2026-05-10 | Spatial clustering MA, tooltip global div, optymalizacja rozmiaru || 2.5.33  | 2026-07-31 | DOKUMENTACJA — wnioski z przeglądu: **Etap 2b HISTORIA I ARCHIWUM oznaczony jako PILNY** (historia jest dziś na tyle nieczytelna, że jest czyszczona do zera, żeby dało się pracować — system zmusza do niszczenia danych potrzebnych do weryfikacji); wymagania: filtry, rozdzielenie aktywne/archiwum, czytelny łańcuch ewolucji w „patchworkowych" ciągach, eksport archiwum. **Decyzja o bazie danych zrewidowana**: nie „gdy wielu synoptyków", tylko gdy historia i weryfikacja zaczną mieć znaczenie — rekomendacja **SQLite** (jeden plik, bez serwera, kopia przez skopiowanie pliku), PostgreSQL/PostGIS dopiero przy C1 i tylko przy realnej potrzebie. Kontener zdarzenia: bez zaszytego horyzontu czasowego (granica W/EW, zdarzenia >7 dni). Odnotowany otwarty problem pozyskania danych obserwacyjnych do weryfikacji |
| 2.5.32  | 2026-07-31 | **INSTRUKCJA UŻYTKOWNIKA** (`INSTRUKCJA_UZYTKOWNIKA.md`) — 15 rozdziałów dla synoptyków: wydanie ostrzeżenia krok po kroku, wybór obszaru i znaczenie zaznaczenia przy aktualizacji (eskalacja = wycinek, reszta zostaje), biblioteka skutków 3+3, sekwencje czasowe, uzgadnianie importu z IMGW API, widok Status z osią czasu, eksporty i materiały dla mediów, reguły wymuszane przez system, skróty, tryb mobilny, diagnostyka. ROADMAP uzupełniony o pozycje przyjęte z recenzji zewnętrznej: wskaźnik pokrycia powiatu w konturze (pomoc, nie decyzja), scenariusz zdarzenia jako kontener roboczy, eksport archiwum pod zewnętrzną weryfikację (POD/FAR/Brier), zasada nierozbudowywania EditorPanel/MapPanel oraz filozofia „proponuj, nie publikuj" |
| 2.5.31  | 2026-07-31 | DOKUMENTACJA: README napisany od nowa (był przy v2.4.4 — stary port 3000, nieaktualne instrukcje) — aktualne uruchomienie, wdrożenie zakładowe z tabelą portów, funkcje, reguły operacyjne wymuszane przez system, struktura projektu, uwagi dla rozwijających; ROADMAP napisany od nowa (był przy v2.3.2 pod starą nazwą „MeteoCAP Editor") — stan etapów 1–5 z pracy operacyjnej, wątki otwarte, horyzont dalszy, audyt przedbuildowy z historią wpadek, które go wymusiły; ARCHITECTURE_VISION → v0.4: sekcja „Stan realizacji" (co potwierdzone, co uściślone przez praktykę) i „Rewizja kolejności" — model konturowy przed reformą typów, bo autorstwo obszaru okazało się wąskim gardłem. Wszystkie dokumenty spójne co do wersji |
| 2.5.30  | 2026-07-31 | **HOTFIX 2.5.29 — import z IMGW API był zepsuty:** nowy kod wcisnął się MIĘDZY dekorator `@app.post("/api/import/imgw/save")` a funkcję zapisu, więc zapis wywoływał analizę (inny format ciała) — stąd „nic nie skutkuje" i błędy w konsoli; funkcja zapisu została bez adresu. Ponowny import po skasowaniu: odwołane ostrzeżenia nadal blokowały `imgw_id` jako „już w bazie" — teraz liczą się tylko obowiązujące (`_live_imgw_ids`). **WYDAJNOŚĆ:** kontekst podglądu zajętości tworzył nowy obiekt przy każdym uderzeniu w klawisz w polu czasu, co przemalowywało 380 powiatów — dodane dławienie (400 ms) i stabilne klucze w zależnościach mapy. **UX:** uzgodnienie importu przeniesione z panelu bocznego DO okna importu (jeden przepływ w jednym miejscu), okna zamykają się po udanym zapisie. Audyt rozszerzony o kontrolę wiązania wszystkich 51 endpointów |
| 2.5.29  | 2026-07-30 | **B11 UZGADNIANIE IMPORTU Z IMGW API** — nowy `/api/import/imgw/analyze` klasyfikuje partię: nowe / już w bazie / nowsza wersja istniejącego / wymaga decyzji. Przyczyna błędu: API nie oznacza rekordów jako nowych WERSJI, więc aktualizacja kolegi 1°→2° kładła się obok istniejącej jedynki i przez 8 h współistniały dwa ostrzeżenia tego samego typu na tym samym powiecie. Ścieżka zapisu importu respektuje teraz regułę nakładania (dotąd omijała ją całkowicie); rekordy dostają pochodzenie (`source=imgw_api`) i znacznik ręcznej modyfikacji — kolizja z rekordem z API i nietkniętym można zastąpić automatycznie, kolizja z pracą synoptyka wymaga decyzji. Dialog w edytorze: „Zastąp stanem z API" / „Zachowaj moje, dodaj resztę" / „Anuluj"; zastępowane ostrzeżenia są odwoływane ze śladem w historii. **B12** wyraźny przycisk „Pobierz aktualne ostrzeżenia z IMGW API" na górze edytora |
| 2.5.28  | 2026-07-30 | **B9a** deduplikacja MeteoAlarm odporna na PRZESUNIĘTE okna — serwisy aktualizujące często (DWD) przy każdej wersji przesuwają czas, więc porównanie okien na równość nie wystarczało (Kreis Harburg: burze St.2 14–20 i 18–23 obok siebie); teraz w obrębie (kraj, region, zjawisko) nowszy `sent` wypiera każdy NACHODZĄCY wpis, sekwencje rozłączne zostają; **B9b** edytor stosuje to samo okno czasu co Status (koniec rozjazdu „w edytorze wiatr, w Statusie wiatr i burze"); **B9c** filtr zjawisk MeteoAlarm w Statusie (wspólny z mapą); throttle suwaka czasu przez requestAnimationFrame (zacinanie przy przeciąganiu); **B14** logo i favicon względem prefiksu — 404 na `/assets/imgw_logo_pl.svg` i `/favicon.ico` |
| 2.5.27  | 2026-07-29 | ETAP 2 (część 1): **B9 MeteoAlarm** — deduplikacja WERSJI po (kraj, region, okno) z wyborem najświeższego `sent` (Bratysława: mapa pokazywała St.2 z 27.07, lista St.3 z 28.07 — teraz zgodnie St.3, a inne okno zostaje jako sekwencja); MA reaguje na suwak czasu i filtr zjawisk, domyślnie okno „teraz + 24 h" (kraje publikują odpowiedniki PRONIEB-ów na tydzień); ikony grupowane per region — jedna z najwyższym stopniem i licznikiem zamiast chmury kółek; **B2** powiaty zajęte przez to samo zjawisko w nachodzącym czasie oznaczane na mapie JUŻ W TRAKCIE zaznaczania; **B3** konflikt 409 zwraca identyfikatory powiatów — panel z „Pokaż na mapie" i „Odznacz kolidujące", powiaty podświetlane na czerwono |
| 2.5.26  | 2026-07-29 | Panel „zmieniasz obszar" w edytorze rozróżnia operacje — przy eskalacji/deeskalacji pokazuje „zmiana obejmie N powiatów, pozostałe zachowają stopień X, nic nie zostanie usunięte" (dotąd poprawiony był tylko dialog potwierdzenia, napis w panelu straszył dalej); **CZAS LOKALNY** we wszystkich grafikach (Europe/Warsaw zamiast UTC) z jawnym dopiskiem „(czas lokalny)" przy znaczniku; legenda w metryczce ustawiona pionowo z twardo zarezerwowanym miejscem — w rzędzie nie mieściła się w panelu 430 px i opisy były ucinane; opisy stopni w stylu czynnościowym (wzór NSWWS): 1° zachowaj ostrożność · 2° przygotuj się · 3° podejmij działania |
| 2.5.25  | 2026-07-29 | HOTFIX: w EditorPanel/MapPanel/StatusView adres API wrócił do gołego `/api` (regresja przy przenoszeniu plików między buildami) — pod prefiksem `/osmet-dev/` dawało to 404 na `/api/counties`, `/api/impacts-library`, `check-level` i puste warstwy mapy. Wszystkie 8 plików frontu liczy adres względem `BASE_URL`; dodana kontrola „czy gdziekolwiek został goły /api" oraz zbiorczy audyt obecności kluczowych zmian z ostatnich wersji |
| 2.5.24  | 2026-07-29 | HOTFIX 2.5.23: brakujący import `useRef` w App.js (historia zaznaczenia z Etapu 1) — aplikacja nie wstawała, biały ekran i `ReferenceError: useRef is not defined`. Dodana rutynowa kontrola: porównanie hooków użytych z zaimportowanymi we wszystkich plikach frontu |
| 2.5.23  | 2026-07-29 | ETAP 1 po dyżurze: **A1** walidacja czasów (koniec musi być po początku — front blokuje zapis i podświetla pole, backend 422); **A2** koniec fałszywego straszaka przy eskalacji — komunikat zależny od operacji („podnosisz stopień na N powiatach, pozostałe zachowają stopień X", nic nie znika); **A4** usunięty artefakt „Brak aktywnych ostrzeżeń" rysowany w punkcie (0,0) w trybie bez kafli; **A5** panel metryczki skalowany do zajętości (koniec pustki i ucinania); **B1** ochrona zaznaczenia: potwierdzenie przy „Wyczyść"/„Cała Polska" powyżej 5 powiatów, historia zaznaczenia z cofaniem (Ctrl+Z / Ctrl+Shift+Z) i przyciskami ↶ ↷ na mapie; legenda stopni wg Tabeli nr 3 instrukcji IMGW (zalecana ostrożność / duża / najwyższa) |
| 2.5.22  | 2026-07-28 | WDROŻENIE ZA WSPÓLNYM PROXY: aplikacja obsługuje prefiks `/osmet-dev/` (vite `base`, nginx kontenera, API względem `BASE_URL` w 8 plikach) — ten sam adres działa przez bramę i bezpośrednio po porcie; port frontendu 3001 → **31001** (zgodnie z Portainerem); komplet apps-proxy w `deploy/` (nginx.conf bez obcinania prefiksu, compose z sieciami external, instrukcja APPS_PROXY.md, strona startowa ze spisem aplikacji); compose dla Case Explorera (31002). UI: plakietka „WERSJA DEWELOPERSKA" w nagłówku i zamykany pasek wyjaśniający, że narzędzie służy do rozwijania koncepcji ostrzeżeń, a tworzone tu ostrzeżenia nie są oficjalnymi ostrzeżeniami IMGW-PIB |
| 2.5.21  | 2026-07-28 | GOSPODARKA MIEJSCEM w wariantach bez kafli („jedno zjawisko" i „sama mapa"): obok mapy pojawia się rozpisana lista zjawisk z PEŁNYMI listami województw, liczbą powiatów i okresem (wzór metryczki); typografia panelu skalowana do liczby pozycji (przy 1–2 zjawiskach pismo ×1,45 — panel przestał świecić pustką: wypełnienie z ~35% do ~60%); panel zwężony do 26% szerokości, odzyskane miejsce dostaje mapa; wszystkie wiersze przycinane do szerokości panelu — zweryfikowane pomiarowo, 0 przekroczeń |
| 2.5.20  | 2026-07-28 | INFOGRAFIKA — poprawki czytelności: każdy wiersz podpisu przycinany do szerokości kafla (długie nazwy zjawisk i listy województw nachodziły na sąsiedni kafel i były urywane), lista województw powyżej 4 zastępowana skrótem „N województw"; liczba kolumn dobierana do liczby zjawisk (2 kolumny do 4 zjawisk, 3 powyżej) — zweryfikowane pomiarowo: 0 przekroczeń szerokości we wszystkich układach; JEDNO ZJAWISKO → brak kafli, sama mapa na pełną szerokość z nagłówkiem zjawiska (kafle nic nie wnosiły); nowy wariant „Sama mapa" (facets=false) — ogólnopolska bez podziału na zjawiska, poziomo i pionowo |
| 2.5.19  | 2026-07-28 | INFOGRAFIKA DLA MEDIÓW: nowy tryb PNG `mode=infographic` — duża mapa zbiorcza + siatka małych map PER ZJAWISKO (każda co najwyżej trójkolorowa: stopnie 1–3), na małych mapach bez granic powiatów (przy tej skali zlewają się w szum), granice województw i pełny podkład 380 powiatów zachowane; typografia skalowana do formatu (tytuł ~52 px) z myślą o czytelności w mediach; trzy orientacje do wyboru: poziomo 1920×1080, pionowo 1240×1754 (A4), 4:5 1080×1350 (social); nagłówki sekcji, legenda stopni, logo IMGW, znacznik czasu; informacja „+N kolejnych zjawisk" gdy siatka nie mieści wszystkich. Selektor formatu i orientacji w Statusie, działa łącznie z filtrem województwa |
| 2.5.18  | 2026-07-28 | EKSPORT PNG NAPISANY OD NOWA (`png_map.py`, Pillow): koniec zawodnej ścieżki SVG→svglib (przycisk „nie działał" i po cichu podmieniał PNG na SVG); mapa rysuje WSZYSTKIE 380 powiatów jako podkład (brak dziur), powiaty z ostrzeżeniem kolorowane wg najwyższego stopnia, obrysy województw, logo IMGW; metryczka: zjawisko/stopień, liczba powiatów, lista województw, okres, legenda stopni; naprawione gubienie powiatów (normalizacja ID przez str + zfill). RAPORTY WOJEWÓDZKIE: selektor województwa w Statusie działa dla PNG i PDF, z przycięciem powiatów do wybranego województwa; błędy eksportu widoczne dla użytkownika. Pillow w requirements |
| 2.5.17  | 2026-07-28 | POPRAWKA RESPONSYWNOŚCI (naprawia 2.5.16): powrót do stałej wysokości 100dvh — poprzednie „auto" zerowało wysokość mapy w Statusie (mapa żyje wewnątrz panelu) i wypychało panel edytora poza ekran bez możliwości przewinięcia. Na wąskich ekranach przełącznik „Mapa / Edytor" pokazuje wybrany widok na PEŁNYM ekranie; w Statusie i Ustawieniach przełącznik ukryty (panel zawsze pełny). Formularz scrolluje się wewnątrz swojego obszaru; wymuszenie invalidateSize po przełączeniu |
| 2.5.16  | 2026-07-27 | RESPONSYWNOŚĆ: układ składa się w pion poniżej 900 px (mapa 55vh + panel pełnej szerokości, scroll całej strony), zagęszczony nagłówek i nawigacja, pola 16 px na telefonie (bez auto-zoomu iOS), węższe nakładki mapy, wariant landscape; `invalidateSize` Leaflet przy obrocie ekranu i zmianie rozmiaru. Desktop bez zmian |
| 2.5.15  | 2026-07-25 | KOREKTY: **jedno źródło wersji** — backend czyta plik VERSION, UI pobiera ją z `/api/version` (koniec rozjazdu numeru: Header miał 2.5.8, API 2.5.11, VERSION 2.5.14); VERSION kopiowany do obrazu (kontekst builda backendu = katalog projektu); **konfigurowalny katalog danych** `OSMET_DATA_DIR` w main.py/delivery.py/webhook.py (umożliwia instalację bez Dockera); CORS bez `*` przy allow_credentials (lista z `OSMET_CORS_ORIGINS`); upraszczanie poligonów do ≤20 wierzchołków (Douglas-Peucker) przy generowaniu CAP — wymóg MeteoAlarm; zapis cache `cap_xml` tylko gdy faktycznie dogenerowano. NOWE: katalog `deploy/` — unit systemd, konfiguracja nginx, instrukcja INSTALL_LINUX.md |
| 2.5.14  | 2026-07-20 | BIBLIOTEKA SKUTKÓW/ZALECEŃ z kreatora: backend czyta matrix z /data/impacts_library.json (nadpisywalny z UI, zapis atomowy) z fallbackiem do wbudowanej selekcji 10.07 (509 wpisów); GET/POST /api/impacts-library; w edytorze pod polami „Skutki" i „Zalecenia" panel doboru: checkboxy linii dla zjawisko×stopień, chipy filtra zakresu (transport/rolnictwo/energetyka/budownictwo), licznik 3+3 z miękkim limitem, „Wstaw do pola" składa punktowaną treść; wgrywanie eksportu JSON z kreatora bez rebuildu |

